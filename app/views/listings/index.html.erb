  <div class="jumbotron">
  <h1>Find the office space you need</h1>
  <p>If you're a freelancer, bla bla blah!</p>
</div>

<div class="row">
</br>
  <% @listings.each do |listing| %>
    <div class="col-md-4">
        <div class="thumbnail">
        <%= link_to image_tag(listing.image.url(:medium)), listing %>
        <div class="caption">
        <h3><%= listing.name %></h3>
        <p> Add by <strong><%= listing.user.name %></strong>  </p>
        <p> <strong> <%= listing.price %> </strong> SAR per workday</p>
        <p><%= listing.city %></p>
      
      </div>
     </div>
   </div>
<% end %>
</div>

<div class="row">
<div class="col-md-12">
      <% if user_signed_in? %>
           <%= link_to 'Add a Workspace', new_listing_path, class: "btn btn-danger center-block" %>
      <% end %>
    </div>
</div>
<hr>
